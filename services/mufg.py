from typing import Dict


def check(company: str, search_type: str, value: str) -> Dict:

    return {
        "success": False,
        "registrar": "MUFG",
        "message": "MUFG integration is not configured yet."
    }
