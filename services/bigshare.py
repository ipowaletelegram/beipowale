from typing import Dict


def check(company: str, search_type: str, value: str) -> Dict:

    return {
        "success": False,
        "registrar": "Bigshare",
        "message": "Bigshare integration is not configured yet."
    }
